<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/item_update"
        android:icon="@drawable/ic_update"
        android:title="@string/update"
        app:showAsAction="ifRoom" />
    <item android:id="@+id/item_share"
        android:icon="@drawable/ic_share"
        android:title="@string/share"
        app:iconTint="@color/White"
        app:showAsAction="ifRoom" />
    <item android:id="@+id/group_help"
        android:icon="@drawable/ic_group_help"
        android:title="@string/group_help_str"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/group_exit"
        android:icon="@drawable/ic_exit_to_app"
        android:title="@string/exit_to_app"
        app:showAsAction="ifRoom" />
</menu>
